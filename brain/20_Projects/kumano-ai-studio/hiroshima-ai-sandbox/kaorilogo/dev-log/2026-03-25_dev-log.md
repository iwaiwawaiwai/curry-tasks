# 開発ログ 2026-03-25

## 完了したこと

### UI改善（プロファイルバー）
- **バー表示を2段構成に変更**
  - タイトル（気分・時間帯など）→ 左端に配置（`axis-title`、11px）
  - min/max端点（鎮静↔活性など）→ バーの上段に左右配置（`axis-endpoints`）
  - バー本体 → パネル幅いっぱいに伸びるよう `flex:1` で確保
  - `align-items: flex-end` でタイトルをバーの真横に揃える
- **使用シーンの表示を縦並びに変更**
  - タイトル「使用シーン」→ 他の軸と同じ 11px
  - 選択値（📍 仕事中・在宅など）→ タイトルの下の行に表示
  - 未選択時は `—`（グレー）

### リポジトリ整理
- `curry-tasks` リポジトリ → `second-brain` に名前変更（GitHub上も）
- `kaorilogo-prototype/` を `projects/kaorilogo-prototype/` に移動
- `kaorilogo-prototype` の独立したGitHubリポジトリを新規作成・push
  - URL: https://github.com/iwaiwawaiwai/kaorilogo-prototype

### brain整備
- `90_System/` → `90_Guides/` にリネーム
- `brain/90_Guides/dev-project-rules.md` 新規作成（開発プロジェクト運用ガイド）

## VSCode フォルダ構成（現在）

```
VSCode/                    ← GitHub: second-brain
├── brain/
├── projects/
│   ├── kaorilogo/         ← 設計資料
│   └── kaorilogo-prototype/ ← GitHub: kaorilogo-prototype（アプリ本体）
└── CLAUDE.md
```

## 次にやること（Railway デプロイ）

**現状：** コードは Railway 対応済み（`process.env.PORT` 使用）。変更不要。

**手順（ここから）：**
1. https://railway.app にアクセス
2. 「Login with GitHub」でログイン
3. 「New Project」→「Deploy from GitHub repo」
4. `iwaiwawaiwai/kaorilogo-prototype` を選択
5. 「Variables」タブ → `ANTHROPIC_API_KEY` を設定
6. 自動デプロイ → URL発行（`xxx.railway.app`）

**ポイント：** 無料枠 $5/月クレジット。ストリーミングもそのまま動く。
